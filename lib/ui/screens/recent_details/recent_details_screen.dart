import 'package:flutter/material.dart';

class RecentDetailsScreen extends StatelessWidget {
  const RecentDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('RecentDetails Screen'),
      ),
    );
  }
}
